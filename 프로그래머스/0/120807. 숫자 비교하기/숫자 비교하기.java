class Solution {
    public int solution(int num1, int num2) {
        // if(num1 == num2) return 1;
        // else return -1;
        
        // * 삼항연산자 사용법
        return (num1 == num2)? 1:-1;
    }
}